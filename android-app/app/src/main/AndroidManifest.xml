<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_fanyu"
        android:label="番遇"
        android:roundIcon="@drawable/ic_launcher_fanyu"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">
        <activity android:name=".MainActivity"
            android:theme="@style/TransparentResolverTheme" />
        <activity android:name=".SiteVerificationActivity" />
        <activity android:name=".SourceManagementActivity" />
        <activity android:name=".SourceEditorActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".PlayerActivity"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
